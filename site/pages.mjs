const page=(group,id,title,purpose,body,examples=[])=>({group,id,title,purpose,body,examples});
export const pages=[
page('Foundations','', 'Foundations','A working reference for Tomorrow We Are studio projects.',`<p>Standards brings together the colors, typography and interface components used in Specimen. Use the live examples, inspect their code, and adopt a version when it fits your project.</p><div class="overview-links"><a href="/components/buttons/"><span>01</span> Components <span>Working controls ↗</span></a><a href="/patterns/gallery/"><span>02</span> Patterns <span>Complete interactions ↗</span></a><a href="/installation/"><span>03</span> Installation <span>Use the package ↗</span></a></div><h2>Principles</h2><p>Let the content lead. Keep controls quiet until they are needed. Prefer clear labels, familiar HTML and deliberate motion. Share interface decisions across studio projects; independent projects can retain their own identity.</p><h2>One source</h2><p>This site and Specimen use the same package. Examples show supported behavior, with synthetic data and local responses.</p>`),
page('Foundations','colors','Colors','Two palettes, with a restrained accent for interaction.',`<div class="swatches">${[['--bg','Background'],['--ink','Foreground'],['--accent','Accent'],['--well','Image well'],['--muted','Secondary text'],['--quiet','Quiet text']].map(([token,label])=>`<div class="swatch"><div style="background:var(${token})"></div><p>${label}</p><code>${token}</code></div>`).join('')}</div><table><thead><tr><th>Token</th><th>Dark</th><th>Light</th></tr></thead><tbody><tr><td>Background</td><td>#1B1B1B</td><td>#F5F4F1</td></tr><tr><td>Foreground</td><td>#EBEAE2</td><td>#575856</td></tr><tr><td>Accent</td><td>#CEC9FF</td><td>#4B40B7</td></tr><tr><td>Well</td><td>#343434</td><td>#EAEAE8</td></tr></tbody></table><h2>Usage</h2><p>Use foreground for primary content, quiet text for secondary metadata, and the accent for hover, focus and the current location. Use <code>--on-accent</code> for text on an accent surface.</p><h2>Accessibility</h2><p>Never communicate state through color alone. Quiet text and subtle focus rings reproduce Specimen’s existing palette; check contrast at the actual size and background before adopting them for essential information.</p>`),
page('Foundations','typography','Typography','One familiar type stack, with hierarchy through spacing and tone.',`<div class="type-specimen"><p class="type-large">Tomorrow<br>We Are</p><p>ABCDEFGHIJKLMNOPQRSTUVWXYZ<br>abcdefghijklmnopqrstuvwxyz<br>0123456789 · &amp; × ↗</p></div><h2>Font stack</h2><pre><code>"PP Neue Montreal", "Neue Montreal", Arial, sans-serif</code></pre><p>Neue Montreal Medium is loaded as a cached WOFF2 webfont, so visitors do not need it installed. Arial keeps text readable while the font loads or if its download fails. Body copy in Specimen is 14px at weight 500, with a 1.4 line height. This documentation gives longer passages more room.</p><h2>Usage</h2><p>Use sentence case for labels. Keep headings concise and avoid introducing extra weights to compensate for weak spacing.</p>`),
page('Foundations','spacing','Spacing','Small, repeatable intervals keep dense interfaces readable.',`<div class="spacing-scale">${[4,8,12,16,24,32,40,64].map(n=>`<div><code>${n}px</code><span style="width:${n}px"></span></div>`).join('')}</div><h2>Usage</h2><p>Buttons use 12px padding on every edge. Common layout gaps are 16px and 24px. Screenshot wells use 12px on mobile, 16px on intermediate screens and 32px on desktop. Gallery rows have a 64px gap.</p><p>Tokens <code>--space-1</code>, <code>--space-2</code>, <code>--space-3</code>, <code>--space-4</code>, <code>--space-6</code>, <code>--space-8</code>, <code>--space-10</code> and <code>--space-16</code> expose this scale.</p>`),
page('Foundations','responsive-layout','Responsive layout','Layout changes when the content needs more room.',`<table><thead><tr><th>Width</th><th>Specimen layout</th></tr></thead><tbody><tr><td>≤ 560px</td><td>Navigation above content; one gallery column</td></tr><tr><td>561–849px</td><td>Navigation above content; two gallery columns</td></tr><tr><td>850–1249px</td><td>340px sidebar; two gallery columns</td></tr><tr><td>≥ 1250px</td><td>400px sidebar; three gallery columns</td></tr></tbody></table><h2>Behavior</h2><p>The viewer covers the entire page below 1250px. At desktop widths the sidebar stays available beside it. Standards uses a smaller 264px desktop sidebar for its documentation links.</p><h2>Accessibility</h2><p>Test at 200% text enlargement and narrow widths. Let navigation wrap, preserve source order, and keep images inside their content column.</p>`),
page('Foundations','borders','Borders','Quiet dividers separate related content without adding panels.',`<p>Use a single pixel of <code>--hairline</code>. Buttons and email fields have square corners. The existing previous, next and close icon controls use circular targets.</p><h2>Usage</h2><p>A divider groups content; it does not supply a label. Use a heading when the distinction needs to be named.</p>`,['border-inventory']),
page('Foundations','icons','Icons','Small inline symbols support explicit text labels.',`<p>Directional icons use a 24 × 24 viewBox, a 1.5px currentColor stroke, and a 16px rendered size. Inline SVG avoids a library or a separate request.</p><h2>Accessibility</h2><p>Decorative SVG uses <code>aria-hidden="true"</code> and <code>focusable="false"</code>. An icon-only button needs an accessible name describing its action. The visible theme symbol is ◐; close uses ×.</p>`,['icon-inventory']),
page('Foundations','motion','Motion','Motion follows the object being moved.',`<table><thead><tr><th>Interaction</th><th>Duration</th></tr></thead><tbody><tr><td><a href="/components/buttons/#button">Control color</a></td><td>120ms</td></tr><tr><td><a href="#motion">Screenshot lift</a></td><td>180ms, ease-in-out</td></tr><tr><td><a href="/components/counters/#counter">Counter</a></td><td>220ms</td></tr><tr><td><a href="/patterns/image-viewer/">Viewer handoff</a></td><td>280ms</td></tr></tbody></table><h2>Behavior</h2><p>The viewer carries the currently painted image into its expanded position, then replaces it with a decoded full image. Reversing direction can interrupt navigation without a blank frame.</p><h2>Accessibility</h2><p>Reduced motion removes the hover lift, UI transitions and counter animation. Change your operating system preference to compare the behavior.</p>`,['motion']),
page('Foundations','accessibility','Accessibility','Preserve native interaction, readable labels and predictable focus.',`<ul><li>Use links for navigation and buttons for actions.</li><li>Give inputs persistent labels and announce asynchronous results with a polite status region.</li><li>Keep Tab order in document order. The signup dialog traps focus and restores the opener on close.</li><li>Escape closes signup and the viewer. The viewer also supports arrow keys, Home and End.</li><li>Keep covered page regions inert, while the visible desktop sidebar remains usable in the viewer.</li><li>Respect reduced motion, browser zoom and data-saving preferences.</li></ul><h2>Before adoption</h2><p>The preserved quiet text and translucent focus ring are subtle. Verify contrast for your context; this release is not a claim of complete WCAG conformance. Disabled controls need nearby context explaining when they become available.</p>`),
page('Foundations','efficient-loading','Efficient loading','Load useful pixels when intent makes them likely to be needed.',`<p>Foundations and plain controls do not need a runtime framework. Import NumberFlow and the gallery patterns only when using them. Bundle the chosen package revision with the application; do not make runtime requests to a package CDN.</p><h2>Image behavior</h2><p>Tiny inline WebP placeholders need no extra request. A decoded thumbnail appears immediately. Screenshot hover waits 100ms before warming a full image; category hover waits 40ms before warming the first viewport, capped at 18 thumbnails. Only the immediate previous and next full images are preloaded.</p><p>Save-Data and 2G disable speculative loading. An explicit click still starts the requested image. Original download URLs are preserved.</p>`),
page('Components','buttons','Buttons','Trigger a primary action with a square, filled control.',`<h2>Usage</h2><p>Use the primary button for actions such as opening signup or submitting the form. There is one size and one filled variant in this release: 12px padding, inherited type and square corners.</p><h2>Behavior</h2><p>Hover and keyboard focus switch the background to the accent. Disabled buttons dim and use a wait cursor. During submission, disable the button and announce progress beside the form.</p><h2>Accessibility</h2><p>Use <code>type="button"</code> outside a submit action. Keep the visible label descriptive. Disabled buttons are removed from keyboard navigation.</p>`,['button','button-states']),
page('Components','text-actions','Text actions','Offer a quieter action or a link alongside primary content.',`<h2>Usage</h2><p>Use links for destinations and buttons for in-place operations. Keep related actions together, as in the viewer’s Copy image, Download and View live controls.</p><h2>Behavior and accessibility</h2><p>Quiet text becomes the accent on hover and keyboard focus. Give new-tab links an understandable destination and use <code>rel="noopener noreferrer"</code>. Announce copy success or failure in a status region.</p>`,['text-actions']),
page('Components','icon-controls','Icon controls','Navigate within a sequence using compact directional buttons.',`<h2>Usage</h2><p>Previous and next use 28px circular targets with 16px icons. Reserve these controls for a context where the sequence is already clear.</p><h2>Behavior and accessibility</h2><p>The viewer wraps at the first and last items. Labels identify the neighboring screenshot. Always give an icon-only button an accessible name and leave enough space between targets.</p>`,['icons']),
page('Components','email-input','Email input','Collect an email address with native format validation.',`<h2>Usage</h2><p>Use a visible label where space allows. The signup pattern supplies a screen-reader label, email autocomplete, an email keyboard hint and a placeholder example.</p><h2>Behavior</h2><p>Validate before the submit callback runs. Invalid input retains its value and receives focus. Server errors appear in the adjacent status message; valid formatting does not guarantee a deliverable address.</p><h2>Accessibility</h2><p>Associate help with <code>aria-describedby</code>. Mark invalid fields with <code>aria-invalid</code>; do not use the placeholder as the only label.</p>`,['email']),
page('Components','theme-toggle','Theme toggle','Switch between the existing light and dark palettes.',`<h2>Usage</h2><p>The label names the next available mode. Dark is the default when no valid preference is saved.</p><h2>Behavior and accessibility</h2><p>The choice persists in local storage. The host supplies its own storage key; Specimen keeps <code>twa-srl-theme</code>. Storage failure does not prevent switching. The control has an accessible label and exposes the light-mode state with <code>aria-pressed</code>.</p>`,['theme']),
page('Components','navigation-items','Navigation items','Show destinations and the current location in a compact list.',`<h2>Usage</h2><p>Keep the destination label on the left and optional count on the right. Use actual links so open-in-new-tab and browser navigation remain available.</p><h2>Behavior and accessibility</h2><p>The current location uses the accent and <code>aria-current="location"</code>. Specimen’s query links preserve unrelated URL parameters and support Back and Forward. This inline comparison switches only its sample selection.</p>`,['navigation']),
page('Components','counters','Counters','Show position in a sequence with an optional animated number.',`<h2>Usage</h2><p>Use the counter in the viewer. Subscriber totals and category counts remain plain text.</p><h2>Behavior and accessibility</h2><p>The package’s optional counter entry imports NumberFlow. The container announces the complete value, while the animated digits are decorative. Reduced motion disables the animation.</p>`,['counter']),
page('Components','dividers','Dividers','Separate groups with a single quiet rule.',`<h2>Usage</h2><p>Use <code>hr.divider</code> for a thematic break. Use a CSS border for a purely visual edge, as in the viewer action rail.</p><h2>Accessibility</h2><p>Do not rely on a faint line to communicate a required distinction. Add a heading when groups need names.</p>`,['divider']),
page('Components','status-messages','Status messages','Explain progress and the result of an action near its control.',`<h2>Usage</h2><p>Keep status copy specific: Subscribing…, You’re subscribed., or Could not subscribe. Try again. Keep errors visible long enough to understand and retry.</p><h2>Behavior and accessibility</h2><p>Use <code>role="status"</code> with a polite live region. Retain entered values on a failed submission. An empty signup status takes up no space.</p>`,['status']),
page('Patterns','sidebar-footer','Sidebar and footer','Keep identity, navigation and utilities together.',`<h2>Usage</h2><p>The sidebar places the project identity first, contextual copy and navigation in the middle, and theme and provenance information at the end. The desktop footer settles at the bottom when space permits.</p><h2>Behavior</h2><p>Navigation flows above the content on mobile. Application-specific counts, dates and carbon estimates are supplied by the host. The demo uses synthetic counts and a link back to Standards.</p>`,['sidebar-preview']),
page('Patterns','gallery','Gallery cards and grid','Browse a collection through previews and concise labels.',`<h2>Usage</h2><p>Pass category and reference data to <code>createGallerySections</code>. Each card has a screenshot opener and a separate link to the original site. Titles are inserted as text.</p><h2>Behavior and accessibility</h2><p>The grid responds from one to three columns. Category navigation mounts only the selected section. The viewer stays within that category, and closing focuses the currently viewed card. Image URLs must be HTTP(S); original-site links must use HTTPS.</p>`,['gallery-preview']),
page('Patterns','signup-dialog','Signup dialog','Collect an email through a focused, recoverable form.',`<h2>Usage</h2><p>Use the shared signup template and pass a <code>submit</code> callback to <code>mountSignup</code>. The host owns its endpoint, storage and success message. The hidden website field preserves Specimen’s honeypot contract.</p><h2>Behavior and accessibility</h2><p>Opening places focus in the email field and makes the background inert. Tab stays inside. Escape, Cancel, the close control and an outside click dismiss the form and restore focus. Pending responses cannot overwrite a later form session.</p>`,['signup']),
page('Patterns','image-viewer','Image viewer','Inspect a screenshot while preserving its place in the collection.',`<h2>Usage</h2><p>Mount the viewer template once and call <code>mountGallery(sections)</code> after rendering the collection. The optional pattern includes category history, intent loading, copy, download and previous/next navigation.</p><h2>Behavior and accessibility</h2><p>On desktop, the sidebar stays interactive. Category selection closes the viewer before changing the gallery; signup closes it before opening the form. Covered regions are inert. Escape closes, arrows navigate, and Home/End select the boundaries. Image copying may be unavailable when browser permissions or CORS prevent it.</p>`,['viewer-preview']),
page('Patterns','placeholders','Placeholders','Keep a preview visible until a sharper image is decoded.',`<h2>Usage</h2><p>Supply a small inline WebP placeholder with the image data. It is clipped to the rectangular image well and blurred while loading. Never blur an already loaded thumbnail again.</p><h2>Behavior and accessibility</h2><p>Promotion replaces the painted image only after decode. A failed full image retains the existing preview and announces the failure. Give the final image meaningful alternative text; decorative placeholder layers are hidden from assistive technology.</p>`,['placeholder']),
page('Patterns','retry-states','Retry states','Keep failure understandable and offer a direct way to try again.',`<h2>Usage</h2><p>If the published collection is unavailable, show a short message and Retry. Do not silently substitute an outdated archive.</p><h2>Behavior and accessibility</h2><p>Specimen’s retry reloads the page and asks its CMS endpoint again. The live example above uses a synthetic response so you can test both outcomes without a service request.</p>`,['retry']),
page('Project','installation','Installation','Adopt a deliberate revision and bundle it with your application.',`<p>The package is released on GitHub. Pin the full commit from a tagged release so later changes arrive only when you choose to upgrade.</p><pre><code>npm install --save-exact github:AnushGopalakrishnan/twa-standards#RELEASE_COMMIT</code></pre><p>Replace <code>RELEASE_COMMIT</code> with the full SHA from the <a href="https://github.com/AnushGopalakrishnan/twa-standards/releases">release receipt</a>.</p><h2>Foundations and components</h2><pre><code>import 'twa-standards/fonts.css';
import 'twa-standards/foundations.css';
import 'twa-standards/components.css';
import { mountTheme } from 'twa-standards';

mountTheme(document.querySelector('.theme-toggle'), {
  storageKey: 'your-project-theme'
});</code></pre><h2>Optional patterns</h2><pre><code>import 'twa-standards/patterns/specimen.css';
import { createGallerySections, mountGallery,
  mountSignup } from 'twa-standards/patterns';</code></pre><p>Include foundations, components, then pattern CSS in that order. Signup and viewer HTML templates are exported under <code>twa-standards/patterns/*.html</code> for build-time inclusion. The optional counter entry is <code>twa-standards/counter</code>. The optional <code>twa-standards/fonts.css</code> entry loads the licensed Neue Montreal Medium webfont from the content host. The font binary is delivered separately from the source package.</p><h2>Upgrade and rollback</h2><p>Review the changelog, update the commit pin and lockfile, then build and test the consuming application in both themes and at its supported widths. Roll back by restoring the previous pin and lockfile.</p>`),
page('Project','changelog','Changelog','A short record of intentional changes.',`<h2>1.2.0 <span class="demo-caption">· 13 September 2026</span></h2><p>Clearer presentation across all component and pattern examples: named states, desktop/mobile references, separate demo controls, copyable code and compact mobile navigation. Shared component behavior is unchanged.</p><h2>1.1.2 <span class="demo-caption">· 9 September 2026</span></h2><p>Faster startup and page switching. Prepare adjacent pages while you read and begin loading on touch, while respecting data-saving preferences.</p><h2>1.1.1 <span class="demo-caption">· 9 September 2026</span></h2><p>Load Neue Montreal Medium for every visitor on Standards and Specimen, with font preloading and a cached WOFF2 asset.</p><h2>1.1.0 <span class="demo-caption">· 9 September 2026</span></h2><p>Documentation now keeps the sidebar in place, prefetches links and updates page content without reloading. Browser history, scroll and keyboard focus are preserved. CSS and JavaScript use versioned caching. Signup gains an optional dispose method for cleanup when leaving a page.</p><h2>1.0.2 <span class="demo-caption">· 9 September 2026</span></h2><p>Keep Escape and Tab working when a pending signup moves focus outside the form. Closing and reopening still discards stale responses.</p><h2>1.0.1 <span class="demo-caption">· 9 September 2026</span></h2><p>Make the first click reliable while optional demo modules load, and prevent automatic analytics injection. Shared Specimen UI is unchanged.</p><h2>1.0.0 <span class="demo-caption">· 9 September 2026</span></h2><p>Initial extraction from Specimen: both palettes, shared controls, signup, gallery rendering and the image viewer. Adds this working reference, generated example snippets and synthetic pattern demonstrations.</p><p>Signup now traps keyboard focus, preserves background inert state, and ignores stale responses after dismissal. Existing theme preference keys, image loading, category URLs and viewer geometry are preserved.</p><p><a href="https://github.com/AnushGopalakrishnan/twa-standards/releases">Tagged releases ↗</a></p>`)
];
export const route = page => page.group==='Project'?`/${page.id}/`:`/${page.group.toLowerCase()}/${page.id? page.id+'/':''}`;
